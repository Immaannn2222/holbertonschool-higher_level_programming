#!/bin/usr/python3
"""Inheritance"""


def is_same_class(obj, a_class):
    """checks if obj is an instance of a class"""

    if type(obj) is a_class:
        return True
    else:
        return False
